name: id_verifier_api
description: Didit session token and decision proxy API for KBSync
version: 0.1.0
environment:
  sdk: ^3.11.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  http: ^1.2.2
  dotenv: ^4.2.0
  googleapis: ^14.0.0
  googleapis_auth: ^2.0.0

dev_dependencies:
  lints: ^6.0.0
